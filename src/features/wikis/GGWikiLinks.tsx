import { WikiLinkComponent } from "~/lib/utils";

type GGWikiLinksProps = {
  appName: string;
} & WikiLinkComponent;

function GGWikiLinks({ appName, incrementWikisNum }: GGWikiLinksProps) {
  return <div></div>;
}

export default GGWikiLinks;
